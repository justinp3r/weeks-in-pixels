on:
  push:
    branches:
      - main

jobs:
  super-lint:
    name: lint code base
    runs-on: ubuntu-latest

    steps:
      - name: checkout code
        uses: actions/checkout@v4.1.4

      - name: use Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'